# Contributing

Please read `CODEOWNERS` and follow commit signing and PR rules.

- Fork or branch from `main`
- Run `make fmt` and `make lint` before commit
- Sign commits (`git commit -S`)
- Include issue reference in PR body (e.g. `Closes #123`)
